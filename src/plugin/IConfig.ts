interface IConfig {
  min: number,
  max: number,
  from: number,
  to: number,
  step: number,
  double: boolean,
  isTips: boolean,
  isMinMax: boolean,
  vertical: boolean,
  scin: string,
  current: string,
}

// eslint-disable-next-line no-undef
export default IConfig;
