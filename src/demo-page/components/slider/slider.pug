mixin slider(data)

  if (data)
    - const { min, max, from, to, step, double, tips, minMax, scale, scaleLimit, vertical, scin } = data
    .example-slider.js-example-slider(data-min=min, data-max=max, data-from=from, data-to=to, 
      data-step=step, data-double=double, data-tips=tips, data-min-max=minMax, 
      data-scale=scale, data-scale-limit=scaleLimit data-vertical=vertical, data-scin=scin)
  else 
    .example-slider.js-example-slider